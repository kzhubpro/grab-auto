workflows:
  android-build:
    name: Android Build
    max_build_duration: 60
    environment:
      node: 18
    scripts:
      - npm install
      - npx expo prebuild
      - cd android
      - ./gradlew assembleDebug
    artifacts:
      - android/app/build/outputs/**/*.apk
